{% extends 'base.html.twig' %}

{% block title %}{{ 'produit.edit'|trans }}{% endblock %}

{% block body %}
    <br/>
    <div>
        <p>
            <a class="text-primary" href="{{ path('app_produit_index') }}">{{ 'produit.show'|trans }}</a> > 
            <a class="text-primary" href="{{ path('app_produit_show', {'id': produit.id}) }}">{{ produit.nom }}</a> > {{ 'produit.modif'|trans }}
        </p>
    </div>

    {{ include('produit/_form.html.twig', {'button_label': 'Update'}) }}


    {{ include('produit/_delete_form.html.twig') }}
{% endblock %}
