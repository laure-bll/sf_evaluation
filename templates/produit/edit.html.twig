{% extends 'base.html.twig' %}

{% block title %}Edit Produit{% endblock %}

{% block body %}
    <br/>
    <div>
        <p>
            <a class="text-primary" href="{{ path('app_produit_index') }}">Produits</a> > 
            <a class="text-primary" href="{{ path('app_produit_show', {'id': produit.id}) }}">{{ produit.nom }}</a> > Modifier
        </p>
    </div>

    {{ include('produit/_form.html.twig', {'button_label': 'Update'}) }}


    {{ include('produit/_delete_form.html.twig') }}
{% endblock %}
