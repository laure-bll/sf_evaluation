{% extends 'base.html.twig' %}

{% block title %}{{ 'produit.new'|trans }}{% endblock %}

{% block body %}
    <br/>
    <div>
        <p>
            <a class="text-primary" href="{{ path('app_produit_index') }}">{{ 'produit.show'|trans }}</a> > {{ 'produit.creer'|trans }}
        </p>
    </div>

    {{ include('produit/_form.html.twig') }}

{% endblock %}
