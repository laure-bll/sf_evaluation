{% block body %}
    <h3>Mes informations</h3>
    <br/>

    <table class="table">
        <tbody>
            <tr>
                <th>Email</th>
                <td>{{ utilisateur.email }}</td>
            </tr>
            <tr>
                <th>Prénom</th>
                <td>{{ utilisateur.prenom }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ utilisateur.nom }}</td>
            </tr>
        </tbody>
    </table>

    <button class="btn btn-secondary">
        <a href="{{ path('app_utilisateur_update', {id: utilisateur.id}) }}">Modifier mes informations</a>
    </button>
{% endblock %}
